<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Amics Tech Login</title>
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>

    <![endif]-->

    <!-- styles -->
    <link href="/stylesheets/style.css" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" >
    <link href="/stylesheets/login.css" rel="stylesheet">

</head>
<body>


<div class="container">

    <!-- Static navbar -->
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">AMICS</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li <% if (menu == 'dashboard') { %>class="active" <% } %>><a href="dashboard"><span class="glyphicon glyphicon-dashboard" aria-hidden="true" /> Dashboard</a></li>
                    <li <% if (menu == 'leaderboard') { %>class="active" <% } %>><a href="leaderboard"><span class="glyphicon glyphicon-stats" aria-hidden="true"> Leaderboard</a></li>
                    <li <% if (menu == 'challenges') { %>class="active" <% } %>><a href="challenges"><span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"> Challenges</a></li>
                    <li <% if (menu == 'progress') { %>class="active" <% } %>><a href="progress"><span class="glyphicon glyphicon-star" aria-hidden="true"> Progress</a></li>
                    <li <% if (menu == 'help') { %>class="active" <% } %>><a href="help"><span class="glyphicon glyphicon-question-sign" aria-hidden="true"> Help</a></li>
                    <li <% if (menu == 'settings') { %>class="active" <% } %>><a href="settings"><span class="glyphicon glyphicon-cog" aria-hidden="true"> Settings</a></li>
                </ul>
            </div><!--/.nav-collapse -->
        </div><!--/.container-fluid -->
    </nav>
</div> <!-- /container -->

</body>
</html>